export function addVentaRowTable(tablaThebody) {
  const $mainRegist = document.getElementById(tablaThebody),
    $rowstable = $mainRegist.querySelectorAll("tr");
  $rowstable.addEventListener("click", e => {
    
  })
}